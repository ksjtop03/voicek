/* ----------------------------------------------------------------------------------------------------
                                         컬럼 추가
---------------------------------------------------------------------------------------------------- */

alter table n_tb_schedule_log add price numeric(4,2);

/* ----------------------------------------------------------------------------------------------------
                                         컬럼명변경
---------------------------------------------------------------------------------------------------- */

alter table 테이블명 rename column 기존컬럼명 to 변경할컬럼명;


/* ----------------------------------------------------------------------------------------------------
                                    컬럼 데이터 타입 변경
---------------------------------------------------------------------------------------------------- */

alter table 테이블명 alter column 컬럼명 type 타입값;
ex) alter table n_tb_schedule_log alter column price type numeric(4,2);


/* ----------------------------------------------------------------------------------------------------
                                   constraint 제약조건 추가
---------------------------------------------------------------------------------------------------- */

alter table editions add constraint foreign_book foreign key (book_id) references books (id);